apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "innovatex-ide.fullname" . }}-test-connection"
  labels:
    {{- include "innovatex-ide.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args:  ['{{ include "innovatex-ide.fullname" . }}:{{ .Values.service.port }}/healthz']
  restartPolicy: Never
